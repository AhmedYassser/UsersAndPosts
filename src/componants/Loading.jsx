import React from 'react'

function Loading() {
  
  // this componant used to display loading page (with some animation)
  // while fetching the API still in progress .


  return (
    <div className="loader">
    </div>

  )
}

export default Loading